server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /uxipsos-pages/_health {
        default_type text/plain;
        return 200 "ok\n";
    }

    location /uxipsos-pages/ {
        try_files $uri $uri/ $uri/index.html =404;
    }
}
